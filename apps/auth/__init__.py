"""
Centralized authentication + authorization app.

NOTE: This app is named `auth` per project convention. It is NOT the same as
`django.contrib.auth` (which remains installed). Prefer importing Django auth
types via `django.contrib.auth...` to avoid ambiguity.
"""
